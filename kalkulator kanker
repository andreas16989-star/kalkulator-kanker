<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Kalkulator Edukasi Risiko Kanker</title>
  <style>
    body { font-family: Arial, sans-serif; background:#f9f9f9; }
    .container {
      max-width: 500px; margin: 40px auto; background:#fff; padding:20px;
      border-radius:12px; box-shadow:0 4px 8px rgba(0,0,0,0.1);
    }
    h2 { text-align:center; color:#2a6ebb; }
    label { display:block; margin-top:10px; font-weight:bold; }
    select, input { width:100%; padding:10px; margin-top:6px; border:1px solid #ccc; border-radius:6px; }
    button {
      margin-top:15px; width:100%; padding:12px; background:#2a6ebb; color:#fff;
      border:none; border-radius:8px; font-size:16px; cursor:pointer;
    }
    #result { margin-top:15px; font-size:16px; font-weight:bold; text-align:center; color:#2a6ebb; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Kalkulator Edukasi Risiko Kanker</h2>
    <p style="text-align:center; font-size:14px; color:#555;">⚠️ Bukan diagnosis medis – hanya edukasi awam</p>
    
    <label>Usia:</label>
    <input type="number" id="age" placeholder="Masukkan usia">
    
    <label>Merokok?</label>
    <select id="smoke">
      <option value="0">Tidak</option>
      <option value="10">Ya</option>
    </select>
    
    <label>Riwayat keluarga kanker?</label>
    <select id="family">
      <option value="0">Tidak</option>
      <option value="15">Ya</option>
    </select>
    
    <button onclick="calculateRisk()">Hitung Risiko</button>
    
    <div id="result"></div>
  </div>

  <script>
    function calculateRisk() {
      let age = parseInt(document.getElementById("age").value) || 0;
      let smoke = parseInt(document.getElementById("smoke").value);
      let family = parseInt(document.getElementById("family").value);

      let score = 0;
      if (age >= 50) score += 20;
      else if (age >= 30) score += 10;
      score += smoke + family;

      let message = "";
      if (score < 10) message = "Risiko Rendah ✅";
      else if (score < 25) message = "Risiko Sedang ⚠️";
      else message = "Risiko Lebih Tinggi 🚨";

      document.getElementById("result").innerHTML = message;
    }
  </script>
</body>
</html>
